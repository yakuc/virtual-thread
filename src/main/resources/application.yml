spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: secret
  #jpa:
  #  show-sql: true
    hikari:
      minimum-idle: 0
      maximum-pool-size: 500
server:
  tomcat:
    threads:
      max: 500
      min-spare: 0

logging:
  level:
    jp.co.yakuc.test: INFO